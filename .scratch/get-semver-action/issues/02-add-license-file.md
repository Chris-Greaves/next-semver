# 02 — Add LICENSE file

**What to build:** A `LICENSE` file at the repo root containing the ISC license text, matching `package.json`'s already-declared `"license": "ISC"`, so the repo is legally usable, forkable, and consumable as an Action.

**Blocked by:** None — can start immediately.

**Status:** ready-for-agent

- [ ] `LICENSE` file exists at the repo root with standard ISC license text
- [ ] Copyright holder/year in the license text matches the project's actual authorship
